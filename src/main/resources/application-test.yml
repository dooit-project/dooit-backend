spring:
  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  auth:
    jwt:
      issuer: https://dooit.test
      secret: test-only-jwt-secret-at-least-32-bytes
      access-token-ttl: PT15M
